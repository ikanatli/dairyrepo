spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: create-drop  #will turn on/off hibernate auto-creation of the tables from the entity objects. none/create-drop
  datasource:
    url: 'jdbc:postgresql://localhost:5432/diary'  #URL of the Postgres DB.
    username: ikanatli
    password: ikanatli
  banner:
    location: classpath:banner.txt


  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

